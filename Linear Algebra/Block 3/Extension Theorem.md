If $W\subseteq V$ and $dim(V) < \infty$ is a subspace and if $L=w_1,...,w_m$ is a linearly independent list of vectors in $W$, then we can extend $L$ to a basis of $V$

If $W$ is a subspace of $V$, $dim(W) \leq dim(V)$ If $dim(W) = dim(V)$, then $W=V$
