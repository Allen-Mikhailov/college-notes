 In $\mathbb{R}^n$  a hyperplane is a $(n-1)$ dimensional subspace

A hyperplane in $\mathbb{R}^n$ is the kernel f a $1\times n$  matrix with at least one pivot (rank 1)

In $\mathbb{R}^3$ a hyperplane is just a plane that passes through the origin.